mixin tags(tags)
  ul
    if tags
      each tag in tags
        if env.helpers.isPopularTag(tag)
          li(style="font-weight: bold")
            a(href='/tag/'+tag)= tag
        else
          li= tag
